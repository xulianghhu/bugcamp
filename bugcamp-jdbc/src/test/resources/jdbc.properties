# Oracle database driver class
jdbc.driver.class=oracle.jdbc.driver.OracleDriver

# database url of bugdb
jdbc.url=jdbc:oracle:thin:@//amogridxp01-scan.us.oracle.com:1523/bugap_adx.us.oracle.com

# common user of bugdb, currently is mine 
jdbc.username=liangx

# password of common user
jdbc.password=Xl000111

# initial pool size
jdbc.initial.pool.size=5

# core pool size
jdbc.core.pool.size=10

# max pool size
jdbc.max.pool.size=20

# retry times if get connection failed
jdbc.retry.times=3

# acquire increment when there is no connection
jdbc.acquire.increment=3

# if connection is idle more than this time, it should be released (seconds)
jdbc.limit.idle.time=60

# sql to test connection
jdbc.test.sql=SELECT 1 FROM DUAL

# test period for test sql (seconds)
jdbc.test.period=300

# if test the connection before use
jdbc.test.before.use=true

# show sql if log enabled
jdbc.show.sql=true
